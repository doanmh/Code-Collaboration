<h1>{{title}}</h1>
<p>Welcome to {{title}}</p>

{{#if user}}
<a href="/createTask" class="btn btn-primary">Create New Task</a>
<br>
<br> 
{{#if tasks}}
<h2>Your tasks</h2>
<div class="list-group">
    {{#each tasks}}
        <a href="/task/{{this._id}}" class="list-group-item">{{this._id}}</a>
    {{/each}}
</div>
{{/if}}
{{/if}}